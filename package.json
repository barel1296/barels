{
  "name": "gros",
  "private": true,
  "version": "0.1.0",
  "description": "GROS — AI Growth Operating System (monorepo)",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm --filter @gros/shared run build && pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm --filter @gros/shared run build && pnpm -r run test",
    "dev:api": "pnpm --filter @gros/api run start:dev",
    "dev:web": "pnpm --filter @gros/web run dev",
    "db:migrate": "pnpm --filter @gros/api run db:migrate",
    "db:seed": "pnpm --filter @gros/api run db:seed",
    "py:test": "cd workers/py && uv run pytest -q",
    "py:lint": "cd workers/py && uv run ruff check .",
    "py:typecheck": "cd workers/py && uv run mypy gros_workers"
  }
}
